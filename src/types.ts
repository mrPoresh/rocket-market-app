export enum ButtonType {
    Event = 'btn-event',
    Link = 'btn-link',
}

export enum ButtonSize {
    Small = 'btn-sm',
    Medium = 'btn-md',
    Large = 'btn-lg',
}

export enum ButtonStyle {
    Accent = 'btn-accent',
    Primary = 'btn-primary',
    Default = 'btn-default',
    Warn = 'btn-warn',
}

export enum ButtonTitleStyle {
    Small = 'btn-title-sm',
    Medium = 'btn-title-md',
    Large = 'btn-title-lg',
}